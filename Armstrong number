<?php

$num = 407;
$original = $num;
$sum = 0;

while($num > 0) {
    $digit = $num % 10;          // last digit
    $sum = $sum + ($digit * $digit * $digit);
    $num = (int)($num / 10);
}

if($sum == $original) {
    echo "Armstrong Number";
} else {
    echo "Not Armstrong Number";
}

?>
